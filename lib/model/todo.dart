class Todo{
  String id;
  String userName;
  String title;
  String description;
  DateTime dateTime;
  bool isDone;

  Todo(this.id, this.userName, this.title, this.description, this.dateTime, this.isDone);
}